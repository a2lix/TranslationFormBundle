{% block a2lix_translations_widget %}
    <div class="a2lix_translationsTabs">
        <ul class="a2lix_translationsTabsLocales nav nav-tabs">
        {% for translationsFields in form %}
            {% set locale = translationsFields.vars.name %}

            <li {% if (app.request.locale == locale) %}class="active"{% endif %} data-toggle="tab">
                <a href=".a2lix_translationLocale-{{ locale }}">
                   {{ locale|capitalize }}
                </a>
            </li>
        {% endfor %}
        </ul>

        <div class="a2lix_translationsTabsContents">
        {% for translationsFields in form %}
            {% set locale = translationsFields.vars.name %}

            <div class="a2lix_translationLocale-{{ locale }} tab-pane {% if (app.request.locale == locale) %}active{% endif %}">
                {{ form_widget(translationsFields) }}
            </div>
        {% endfor %}
        </div>
    </div>
{% endblock %}

{% block a2lix_translations_gedmo_widget %}
    <div class="a2lix_translationsTabs">
        <ul class="a2lix_translationsTabsLocales nav nav-tabs">
        {% for translationsLocales in form %}
            {% set isDefaultLocale = ('defaultLocale' == translationsLocales.vars.name) %}

            {% for translationsFields in translationsLocales %}
                {% set locale = translationsFields.vars.name %}

                <li {% if (app.request.locale == locale) %}class="active"{% endif %} data-toggle="tab">
                    <a href=".a2lix_translationLocale-{{ locale }}">
                        {{ locale|capitalize }} {% if isDefaultLocale %}[Default]{% endif %}
                    </a>
                </li>
            {% endfor %}
        {% endfor %}
        </ul>

        <div class="a2lix_translationsTabsContents">
        {% for translationsLocales in form %}
            {% for translationsFields in translationsLocales %}
                {% set locale = translationsFields.vars.name %}

                <div class="a2lix_translationLocale-{{ locale }} tab-pane {% if (app.request.locale == locale) %}active{% endif %}">
                    {{ form_widget(translationsFields) }}
                </div>
            {% endfor %}
        {% endfor %}
        </div>
    </div>
{% endblock %}